# Production build — served from the same origin as the API, so these are
# relative rather than pointing at localhost.
VITE_API_URL=/api/v1
VITE_STATIC_URL=/static
VITE_APP_NAME=Blackstone
VITE_ENV=production
